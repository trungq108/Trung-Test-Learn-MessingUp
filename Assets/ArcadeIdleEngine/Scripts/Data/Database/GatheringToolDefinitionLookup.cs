using ArcadeBridge.ArcadeIdleEngine.Gathering;
using UnityEngine;

namespace ArcadeBridge.ArcadeIdleEngine.Data.Database
{
	[CreateAssetMenu(menuName = nameof(ArcadeIdleEngine) + "/" + nameof(Data) + "/" + nameof(GatheringToolDefinitionLookup))]
	public class GatheringToolDefinitionLookup : IndexLookup<GatheringToolDefinition>
	{
		
	}
}
